# Big-O (conceitual)
# Cenário. Um algoritmo roda em um segundo quando testado com mil registros. Em produção, a base tem um milhão. Antes de descobrir o problema no pior momento possível, é melhor saber prever como o tempo de execução vai se comportar.

# Tarefa: Explique, com exemplos curtos, como o tempo de execução muda quando o tamanho da entrada dobra, para algoritmos O(n), O(n²) e O(log n).

# Mapeamento do comportamento das complexidades assintóticas quando n dobra (n -> 2n)

# O(n) - Linear: O tempo de execução [LACUNA_1].
# Exemplo: Percorrer uma lista simples para encontrar um item.
tempo_linear_com_dobro = "O tempo de execução se mantém"

# O(n²) - Quadrático: O tempo de execução multiplica por [LACUNA_2].
# Exemplo: Dois laços for aninhados percorrendo a mesma lista.
tempo_quadratico_com_dobro = "O tempo de execução é multiplicado por 4"

# O(log n) - Logarítmico: O tempo de execução aumenta apenas [LACUNA_3].
# Exemplo: Busca binária em uma lista ordenada.
tempo_logaritmico_com_dobro = "O tempo aumenta em uma quantidade constante (1 passo extra)"